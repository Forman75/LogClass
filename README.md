# LogClass: Классификатор юридической значимости цифровых следов

**LogClass** — это программный инструментарий, разработанный в рамках научного исследования по теме «Классификация типов цифровых следов в сетевых журналах и их юридическая значимость».

Программа предназначена для автоматизированного анализа лог-файлов (Web, Proxy, VPN), их нормализации и оценки доказательственной силы событий на основе формализованных критериев.

## 🚀 Функциональность

*   **Парсинг логов:** Поддержка форматов Nginx/Apache (Web), Squid (Proxy) и OpenVPN.
*   **Юридический скоринг:** Автоматическое присвоение баллов каждому событию на основе наличия ключевых атрибутов (User ID, IP, Time, Auth).
*   **Классификация:** Разделение событий на 4 класса значимости (A, B, C, D).
*   **Корреляция:** Объединение разрозненных событий в сессии для повышения их доказательственной ценности.
*   **Генерация данных:** Модуль для создания синтетических логов, моделирующих киберинциденты (для отладки и обучения).
*   **Отчетность:** Экспорт результатов в CSV и генерация сводных отчетов в Markdown.

## 📂 Структура проекта

*   `main.py` — Точка входа, графический интерфейс (GUI) на Tkinter.
*   `classifier.py` — Логика оценки и присвоения классов значимости.
*   `models.py` — Структуры данных (LogEvent, Session).
*   `parsers.py` — Модули разбора строк логов различных форматов.
*   `generator.py` — Генератор синтетических данных для экспериментов.
*   `config_manager.py` — Управление весовыми коэффициентами и настройками.
*   `reports.py` — Модуль экспорта и визуализации статистики.

## 🛠 Установка и запуск

Для работы требуется установленный интерпретатор **Python 3.8+**.

1. **Клонируйте репозиторий:**
   ```
   git clone https://github.com/your-username/log-classifier.git
   cd log-classifier
   ```

2. **Установите зависимости:**
   ```
   pip install -r requirements.txt
   ```

3. **Запустите приложение:**
   ```
   python main.py
   ```

## 📊 Методика классификации

В основе алгоритма лежит балльная оценка атрибутов события. События ранжируются по классам:

| Класс | Описание | Юридическая сила |
| :--- | :--- | :--- |
| **A** | **Сильные следы** | Однозначная идентификация субъекта (например, VPN-сессия с логином). Может выступать прямым доказательством. |
| **B** | **Средние следы** | Есть привязка к IP или пользователю, но неполный контекст. Требует верификации. |
| **C** | **Слабые следы** | Технические данные (только IP, NAT). Могут служить лишь косвенными уликами. |
| **D** | **Вспомогательные** | Шумовые события, отсутствуют метки времени. Юридически ничтожны. |

## 🧪 Пример использования

1. Запустите программу.
2. Нажмите **"Сгенерировать учебные логи"** для создания тестового набора данных.
3. Загрузите сгенерированные файлы (web, proxy, vpn) по очереди.
4. Программа автоматически классифицирует события и отобразит статистику.
5. Используйте вкладку **"Сессии"** для просмотра цепочек событий.
